<!DOCTYPE html>
<html>
  <head>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.5/css/bootstrap.min.css">
    <title>The Digest</title>
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
    <%= csrf_meta_tags %>
  </head>

  <body>
    <nav class="navbar navbar-default navbar-static-top">

      <div class="container">
        <div class="navbar-header">
          <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <a class="navbar-brand" href="#">News digest</a>
        </div>

        <div id="navbar" class="navbar-collapse collapse">
        <!--<form class="bs-example bs-example-form" data-example-id=input-group-with-button>-->
        	<ul class="nav navbar-nav pull-right">
            <% if (current_user != nil) %>
              <li class="active"><%= link_to 'Home', articles_path %></li>
              <li class="active"><%= link_to 'My Interests', interests_path(current_user) %></li>
              <li class="active"><%= link_to 'Manage My Account', edit_user_path(current_user) %></li>
              <li class="active"><%= link_to 'Email Me News', requestArticles_path %></li>
              <li class="active"><%= link_to 'Log out', logout_path, method: :delete %></li>
            <% end %>
            
            
          </ul>
        <!--</form>-->
        </div>

      </div>
    </nav>

    <div class="container">

      <% if (current_user) %>
        <%= form_for(:search, url: search_path) do |f| %>
          <div class="input-group">
            <%= f.text_field :query, class: 'form-control', placeholder: "Search for..."%>
            <span class="input-group-btn">
              <%= submit_tag "Search!", class: 'btn btn-default' %>
            </span>
          </div>
        <% end %>
      <% end %>

      <%= yield %>
    </div>

  </body>
</html>
